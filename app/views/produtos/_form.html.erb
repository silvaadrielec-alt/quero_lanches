<%= form_with(model: produto) do |form| %>
  <% if produto.errors.any? %>
    <div style="color: red">
      <h2><%= produto.errors.count %> <%= produto.errors.count > 1 ? "erros impediram" : "erro impediu" %> de salvar este produto:</h2>
      <ul>
        <% produto.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, style: "display: block" %>
    <%= form.text_field :nome %>
  </div>

  <%# Campo de Custo (O que você paga/gasta) %>
<div style="margin-bottom: 20px;">
  <%= form.label :valor_unitario, "Valor de Custo (Compra/Produção)" %>
  <%= form.number_field :valor_unitario, step: 0.01, class: "form-control" %>
</div>

<%# Campo de Venda (O que o aluno paga) %>
<div style="margin-bottom: 20px;">
  <%= form.label :preco_venda, "Preço de Venda" %>
  <%= form.number_field :preco_venda, step: 0.01, class: "form-control" %>
</div>

 <div class="field" style="margin-bottom: 20px;">
  <%= form.label :categoria, "Tipo do Item (Venda ou Insumo)", style: "display: block; font-weight: bold;" %>
  <%= form.select :categoria, ["Venda", "Insumo"], {}, id: "select-tipo-produto", class: "input-estilizado", style: "width: 100%; padding: 8px; border-radius: 10px; border: 1px solid #ccc;" %>
 </div>

 <div id="quadro-inventario" style="background: #fdf8f5; padding: 20px; border-radius: 12px; border: 1px solid #eeb989; margin-bottom: 25px;">
    <h5 style="margin-top: 0; color: #D71920; margin-bottom: 15px;">Controle de Inventário e Unidades</h5>
    
    <div style="display: flex; gap: 15px; flex-wrap: wrap; margin-bottom: 15px;">
      <div style="flex: 2; min-width: 200px;">
        <%= form.label :unidade_medida, "Como você conta este item?", style: "font-weight: bold; display: block; margin-bottom: 5px;" %>
        <%= form.select :unidade_medida, 
            ['unidade', 'fatia', 'folha', 'rodela', 'kg', 'litro', 'gramas', 'ml'], 
            { prompt: 'Selecione a unidade' }, 
            { style: "width: 100%; padding: 8px; border-radius: 8px; border: 1px solid #ccc;" } %>
      </div>
    </div>

    <div style="display: flex; gap: 15px; flex-wrap: wrap;">
      <div style="flex: 1; min-width: 140px;">
        <%= form.label :estoque_centro, "Qtd. Matriz", style: "font-weight: bold; display: block; margin-bottom: 5px;" %>
        <%= form.number_field :estoque_centro, step: :any, style: "width: 100%; padding: 8px; border-radius: 8px; border: 1px solid #ccc;", placeholder: "Ex: 50" %>
      </div>

      <div style="flex: 1; min-width: 140px;">
        <%= form.label :estoque_pavilhao_novo, "Qtd. Pav. Novo", style: "font-weight: bold; display: block; margin-bottom: 5px;" %>
        <%= form.number_field :estoque_pavilhao_novo, step: :any, style: "width: 100%; padding: 8px; border-radius: 8px; border: 1px solid #ccc;", placeholder: "Ex: 30" %>
      </div>

      <div style="flex: 1; min-width: 140px;">
        <%= form.label :estoque_pavilhao_antigo, "Qtd. Pav. Antigo", style: "font-weight: bold; display: block; margin-bottom: 5px;" %>
        <%= form.number_field :estoque_pavilhao_antigo, step: :any, style: "width: 100%; padding: 8px; border-radius: 8px; border: 1px solid #ccc;", placeholder: "Ex: 20" %>
      </div>
    </div>

    <p style="margin-top: 15px; font-size: 0.85em; color: #666; line-height: 1.4;">
      <strong>💡 Dica de Ouro:</strong> Para facilitar, converta o que comprou para a unidade de uso. <br>
      <em>Exemplo: Se comprou 1kg de queijo e ele rende 50 fatias, registre "50" na unidade correspondente.</em>
    </p>
  </div>

 <div style="margin-top: 35px; text-align: center;">
    <%= form.submit "SALVAR ITEM NO ESTOQUE", class: "btn-quero", style: "padding: 15px 40px; cursor: pointer; border: none; width: 100%; max-width: 400px;" %>
    
    <div style="margin-top: 25px;">
      <%= link_to "← VOLTAR PARA O ESTOQUE", produtos_path, style: "color: #007bff; text-decoration: underline; font-weight: 400;" %>
    </div>
  </div>
<% end %>
<script>
  document.addEventListener('turbo:load', function() {
    const selectTipo = document.getElementById('select-tipo-produto');
    const campoVenda = document.getElementById('campo-preco-venda');
    const quadroEstoque = document.getElementById('quadro-inventario');

    if (!selectTipo) return; // Segurança caso o elemento não exista

    function gerenciarCampos() {
      if (selectTipo.value === 'Venda') {
        campoVenda.style.display = 'block';   // Mostra Preço
        quadroEstoque.style.display = 'none'; // Esconde Unidades
      } else if (selectTipo.value === 'Insumo') {
        campoVenda.style.display = 'none';    // Esconde Preço
        quadroEstoque.style.display = 'block';// Mostra Unidades
      } else {
        campoVenda.style.display = 'none';
        quadroEstoque.style.display = 'none';
      }
    }

    selectTipo.addEventListener('change', gerenciarCampos);
    gerenciarCampos(); // Executa ao abrir a tela (importante para edição)
  });
</script>